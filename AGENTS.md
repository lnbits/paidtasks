# PaidTasks — Agent Notes

This extension is a WASM-only template.

For AI/agent guidance, use:

- `lnbits/extensions/wasm/docs/agents_wasm_extensions.md`

Key rule: **Only edit files inside `lnbits/extensions/paidtasks/`. Do not change core files.**
